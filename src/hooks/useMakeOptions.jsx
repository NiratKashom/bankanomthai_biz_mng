import { useState, useEffect } from 'react'

function useMakeOptions(data, key, value) {

	const [optionList, setOptionList] = useState([])

	useEffect(() => {


	}, [data])


	return
}

export default useMakeOptions